import React, { ReactNode } from "react";

export const EmbedWrapper: React.FC<{ children: ReactNode }> = ({ children }) => (
  <>
    {children}
    {/* TODO: Add some fancy code4ro footer here */}
  </>
);
